image: docker:20

stages:
  - build
  - deploy

services:
  - docker:dind

Build-Dev:
    stage: build
    only:
        - develop
    script: 
        - echo "hi"

Build-Prd:
    stage: build
    only:
        - main
    script: 
        - echo "hi"
